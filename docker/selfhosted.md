# Self-Hosted

Use the public engine as a local API or a small CLI service.

## Run

```bash
cargo run -p puffy-cli -- serve
```

## API

- `GET /health`
- `GET /api/health`
- `POST /api/extract`
- `GET /api/jobs/:id`
- `GET /api/assets`
- `GET /api/search`

